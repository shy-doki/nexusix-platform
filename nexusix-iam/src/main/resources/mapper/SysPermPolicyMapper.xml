<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shy.nexusix.iam.mapper.SysPermPolicyMapper">

    <!-- 用户权限策略关联数据结果映射 -->
    <resultMap id="UserPermJoinResultMap" type="com.shy.nexusix.iam.dto.UserPermJoinDTO">
        <result column="policyId" property="policyId"/>
        <result column="permId" property="permId"/>
        <result column="policyStatus" property="policyStatus"/>
        <result column="accessType" property="accessType"/>
        <result column="fieldOperates" property="fieldOperates"/>
        <result column="tableName" property="tableName"/>
        <result column="permCode" property="permCode"/>
    </resultMap>

    <!-- 查询用户在当前租户下的全部权限策略数据（两路径汇聚：ROLE / USER） -->
    <!-- 用户权限 = 用户当前登录租户中所属角色具有的权限 + 用户当前登录租户中该用户本身具有的权限 -->
    <!--
        TENANT路径（target_type='TENANT'）是租户能力边界约束，不作为用户直接权限参与计算。
        用户实际权限仅来自以下两条路径：
        - ROLE路径：target_type='ROLE'，target_id 为 sys_role_policy.id（角色策略ID）
        - USER路径：target_type='USER'，target_id 为 sys_user_policy.id（用户策略ID）
    -->
    <select id="queryUserPermJoin" resultMap="UserPermJoinResultMap">
        SELECT
            pp.id             AS policyId,
            pp.perm_id        AS permId,
            pp.status         AS policyStatus,
            pp.access_type    AS accessType,
            pp.field_operates AS fieldOperates,
            pp.table_name     AS tableName,
            p.perm_code       AS permCode
        FROM sys_perm_policy pp
        INNER JOIN sys_perm p ON pp.perm_id = p.id AND p.is_deleted = 'NOT_DELETED'
        WHERE pp.is_deleted = 'NOT_DELETED'
          AND (
              <!-- 角色级策略：用户在当前租户中所属角色具有的权限 -->
              <!-- target_id 为角色策略ID，通过 sys_role_policy 找到用户关联的角色策略 -->
              (pp.target_type = 'ROLE' AND pp.target_id IN (
                  SELECT rp.id
                  FROM sys_role_policy rp
                  INNER JOIN sys_role r ON r.id = rp.role_id AND r.tenant_id = #{tenantId} AND r.is_deleted = 'NOT_DELETED'
                  WHERE rp.target_type = 'USER'
                    AND rp.target_id = #{userPolicyId}
                    AND rp.is_deleted = 'NOT_DELETED'
                    AND rp.status = 'ACTIVE'
              ))
              OR
              <!-- 用户级策略：用户在当前租户中本身具有的权限 -->
              <!-- target_id 为用户策略ID（sys_user_policy.id） -->
              (pp.target_type = 'USER' AND pp.target_id = #{userPolicyId})
          )
    </select>

</mapper>
